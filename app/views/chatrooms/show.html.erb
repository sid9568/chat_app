<h1>Chatroom</h1>
<div id="messages">
<%= render 'messages/message' %>

</div>

<%= form_with(model: Message.new, remote: true) do |form| %>
  <div class="field">
    <%= form.text_area :content, rows: 4 %>
  </div>
  <div class="actions">
    <%= form.submit 'Send' %>
  </div>
<% end %>
